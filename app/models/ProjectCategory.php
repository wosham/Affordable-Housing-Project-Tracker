<?php
class ProjectCategory extends Model
{
    protected static string $table = 'project_categories';
    // Columns: id, name, slug, icon, color
    // Values: AHP, Markets, Institutional, Schools, Infrastructure
}
