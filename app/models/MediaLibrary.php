<?php
class MediaLibrary extends Model
{
    protected static string $table = 'media_library';
    // Columns: id, filename, original_name, path, url, type, size,
    //          alt_text, uploaded_by, created_at, folder
}
